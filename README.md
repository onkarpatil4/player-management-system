# player-management-system
Developed a RESTful Player Management System that enables efficient handling of player records through CRUD operations. The system allows users to add, view, update, and delete player details such as name, team, goals, and matches. It uses a SQL database for persistent storage and ensures smooth data interaction through API endpoints.
